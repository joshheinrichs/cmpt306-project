using UnityEngine;
using System.Collections;

/**
 * The set of possible directions off of which a room can branch to other rooms.
 * This enum is mainly used in level generation.
 */
public enum Direction {
	UP, DOWN, LEFT, RIGHT
}
